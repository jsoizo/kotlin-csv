name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  linux:
    name: Linux, JVM, JS, and coverage
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup build environment
        uses: ./.github/actions/setup-build

      - name: Run Linux, JVM, JS checks and generate coverage report
        run: |
          ./gradlew clean jvmTest jsNodeTest wasmJsNodeTest linuxX64Test compileKotlinLinuxArm64 compileTestKotlinLinuxArm64 compileKotlinWasmWasi checkKotlinAbi koverXmlReport dokkaGeneratePublicationHtml

      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@v4
        with:
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true

  apple:
    name: Apple Native
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup build environment
        uses: ./.github/actions/setup-build

      - name: Run Apple Native checks
        run: |
          ./gradlew macosArm64Test iosSimulatorArm64Test compileKotlinIosArm64 compileTestKotlinIosArm64

  windows:
    name: Windows Native
    runs-on: windows-2025

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup build environment
        uses: ./.github/actions/setup-build

      - name: Run Windows Native checks
        run: ./gradlew.bat mingwX64Test
